<h1>All articles</h1>


<table>
	<thead>
		<th>Title</th>
		<th>Content</th>
		<th>Show</th>
	</thead>

	<% @articles.each do |i| %>
		<tr>
			<td><%= i.title %></td>
			<td><%= i.content %></td>
			<td><%= link_to 'Show', article_path(i) %></td>
		</tr>
	<% end %>
</table>